from .. import models

def download(file_id):
    """
    # Obviously change this
    company, _ = models.Company.objects.get_or_create(name="aaaaaa")
    models.Report(
        company=company,
        submission_date=timezone.now(),
    ).save()
    """
    pass
